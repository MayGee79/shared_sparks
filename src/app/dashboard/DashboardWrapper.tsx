'use client';

// Import the DashboardContent from the SAME directory
import DashboardContent from './DashboardContent';

export default function DashboardWrapper() {
  return <DashboardContent />;
}
